---
    const {class: className, id} = Astro.props;
---
<section
    id={id}
    data-section = {id}
    class={`section ${className} w-full mx-auto container lg:max-w-7xl md:max-w-2xl pt-20 lg:pt-30`}    
>
    <slot />
</section>